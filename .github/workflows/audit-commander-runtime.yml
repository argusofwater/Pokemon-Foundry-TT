name: Audit Commander runtime

on:
  workflow_dispatch:
  push:
    branches:
      - codex/foundry-base-pass
    paths:
      - "src/module/commander/**"
      - "src/scripts/hooks/init.js"
      - "tools/audit-commander-runtime.mjs"
      - "tools/commander/**"
      - ".github/workflows/audit-commander-runtime.yml"
      - "package.json"
      - "system.json"

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Run system validation
        run: npm run validate

      - name: Run Commander runtime audit
        run: npm run audit:commander-runtime

      - name: Test Pokémon experience progression
        run: npm run test:commander-progression

      - name: Test canonical Pokémon moves
        run: npm run test:commander-moves
